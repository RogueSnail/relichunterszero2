/// @description Clear every 5 frames with no events

if (eventCount > 0 && stepsWithoutEvents = 5) {

	//reset vars
	stepsWithoutEvents = 0;
	eventCount = 0;
	
	//clear arrays
	removedInstances = 0;
	removedInstances = [];
}
